import styled from 'styled-components'

export const StyledArticles = styled.div`
    .articlesHandler {
      max-width: 750px;
      height: 50px;
      display: flex;
      justify-content: space-between;
      
      .sortWrapper div.select {
        height: 40px;
        font-size: 15px;
      }
    }
`