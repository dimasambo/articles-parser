import styled from 'styled-components'

export const StyledForm = styled.form`
  div.search-input {
    transform: scale(0.67) translateY(-37px);
    width: 400px;
    
    label {
      font-size: 20px;
    }
  }
  
  .search-button {
    transform: translateX(-67px);
    font-size: 13px;
    padding: 7px 14px;
  }
`